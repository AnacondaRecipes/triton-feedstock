# Higher version needed for aarch64
# This is not currently supported on our CI machines
# Await support for this before building for aarch64
# c_stdlib_version: # [linux and aarch64]
#   - 2.34          # [linux and aarch64]

# Aligned with pytorch 2.12.0 CUDA matrix; cuda-nvcc 12.9 + 13.0 are in pkgs/main.
cuda_compiler_version:
  - 12.9
  - 13.0
